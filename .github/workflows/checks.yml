name: Linters

on:
  push:
    branches: [ master ]

jobs:
  linters:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install deps
        run: pip install black isort flake8 Flake8-pyproject

      - name: Run black
        run: black --check app

      - name: Run isort
        run: isort --check-only app

      - name: Run flake8
        run: flake8 app
